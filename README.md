# Building from CoffeeScript

* Download Node.js from the web
* Open Git Bash
* Type `npm install -g coffee-script` to install CoffeeScript 
* Type `./make.sh` to convert the `.coffee` files into `.js` files